import java.util.*; 

public class ActionController {
    Queue<String> actionQueue = new LinkedList<String>();
    
    public Queue<String> calculateNextActions(SampleHolder samples, MoleculeHolder molecules) {
        if (samples.size == 0) {
            
        } else {
            for (Sample sample : samples.getSamples()) {
            
            }
        }
        return actionQueue;
    }
}
